# mcpwarp Homebrew tap

```sh
brew tap mcpwarp/tap
brew trust --tap mcpwarp/tap   # Homebrew 6+ requires trusting non-official taps once
brew install --cask mcpwarp
```

Upgrade with `brew upgrade --cask mcpwarp`.

Casks are published here automatically by the release workflow in mcpwarp/cli.
